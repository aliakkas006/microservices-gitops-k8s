apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-api-deployment
  namespace: development
  labels:
    app: email-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: email-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: email-api
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - email-api
                topologyKey: kubernetes.io/hostname
      restartPolicy: Always
      containers:
        - name: email-api
          image: aliakkas006255/email-service-api:2a3f8b4400aa25eda51a2255102af11ed8c21674
          securityContext:
            allowPrivilegeEscalation: false
          imagePullPolicy: Always
          ports:
            - containerPort: 7000
          resources:
            requests:
              memory: '300M'
              cpu: '100m'
            limits:
              memory: '500M'
              cpu: '200m'
          env:
            - name: TZ
              value: 'UTC'
          envFrom:
            - configMapRef:
                name: email-api-configmap
            - secretRef:
                name: email-api-secret
          startupProbe:
            httpGet:
              path: health
              port: 7000
            failureThreshold: 15
            periodSeconds: 20
            timeoutSeconds: 3
            initialDelaySeconds: 60
          readinessProbe:
            httpGet:
              path: health
              port: 7000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          livenessProbe:
            httpGet:
              path: health
              port: 7000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
